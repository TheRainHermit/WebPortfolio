<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
		<title>Artist Portfolio - Creative Works</title>
	</head>
	<body>
		<div class="rain-overlay"></div>
		<slot />
	</body>
</html>

<style is:global>
	html,
	body {
		margin: 0;
		padding: 0;
		width: 100%;
		height: 100%;
		font-family: 'Inter', sans-serif;
		background: linear-gradient(135deg, #0f172a 0%, #1e293b 50%, #334155 100%);
		color: #e2e8f0;
		line-height: 1.6;
		overflow-x: hidden;
	}

	* {
		box-sizing: border-box;
	}

	:root {
		--color-primary: #64748b;
		--color-secondary: #475569;
		--color-accent: #94a3b8;
		--color-success: #22d3ee;
		--color-warning: #fbbf24;
		--color-error: #ef4444;
		--color-neutral-50: #f8fafc;
		--color-neutral-100: #f1f5f9;
		--color-neutral-200: #e2e8f0;
		--color-neutral-300: #cbd5e1;
		--color-neutral-400: #94a3b8;
		--color-neutral-500: #64748b;
		--color-neutral-600: #475569;
		--color-neutral-700: #334155;
		--color-neutral-800: #1e293b;
		--color-neutral-900: #0f172a;
		--spacing-unit: 8px;
		--glass-bg: rgba(15, 23, 42, 0.7);
		--glass-border: rgba(148, 163, 184, 0.2);
		--rain-color: rgba(148, 163, 184, 0.3);
	}

	.rain-overlay {
		position: fixed;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		pointer-events: none;
		z-index: 1;
		background: 
			linear-gradient(90deg, transparent 98%, var(--rain-color) 100%),
			linear-gradient(90deg, transparent 40%, var(--rain-color) 41%, var(--rain-color) 42%, transparent 43%),
			linear-gradient(90deg, transparent 65%, var(--rain-color) 66%, var(--rain-color) 67%, transparent 68%);
		background-size: 25px 100%, 15px 100%, 20px 100%;
		animation: rain 0.3s linear infinite;
		opacity: 0.4;
	}

	@keyframes rain {
		0% { background-position: 0% 0%, 0% 0%, 0% 0%; }
		100% { background-position: 25px 100vh, 15px 100vh, 20px 100vh; }
	}

	h1, h2, h3, h4, h5, h6 {
		line-height: 1.2;
		margin: 0;
		color: #f1f5f9;
	}

	p {
		margin: 0;
		line-height: 1.5;
		color: #cbd5e1;
	}

	a {
		text-decoration: none;
		color: inherit;
	}

	button {
		font-family: inherit;
		border: none;
		background: none;
		cursor: pointer;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 var(--spacing-unit);
		position: relative;
		z-index: 2;
	}

	@media (min-width: 768px) {
		.container {
			padding: 0 calc(var(--spacing-unit) * 2);
		}
	}

	@media (min-width: 1024px) {
		.container {
			padding: 0 calc(var(--spacing-unit) * 3);
		}
	}
</style>